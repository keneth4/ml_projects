mediapipe:
  pose:
    min_detection_confidence: 0.8
    min_tracking_confidence: 0.8

pose_estimation:
  curl_counter:
    min_angle: 20
    max_angle: 150
    start_pose_image_path: src/assets/curl_start_pose.png

video_capture:
  device: 0
  flip: true
  show_landmarks: false

interface:
  window_name: AI Gym Tracker
  stats_bar:
    background_color:
      r: 0
      g: 0
      b: 0
    position_top: true
  counter:
    font: FONT_HERSHEY_DUPLEX
    font_scale: 4
  message:
    font: FONT_HERSHEY_SIMPLEX
    font_scale: 2
  title:
    font: FONT_HERSHEY_SIMPLEX
    font_scale: 3
  sets:
    font: FONT_HERSHEY_SIMPLEX
    font_scale: 2

logging:
  level: INFO

